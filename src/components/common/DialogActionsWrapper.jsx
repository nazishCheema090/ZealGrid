import { DialogActions } from '@mui/material';


const DialogActionsWrapper = ({children}) => {
  return (
    <DialogActions>
        {children}
    </DialogActions>
  )
}

export default DialogActionsWrapper